@model EPMS.Web.ViewModels.JobTitle.JobTitleListViewModel

@{
    ViewBag.Title = EPMS.Web.Resources.HR.JobTitle.IndexLabel;
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.MessageVM = TempData["message"];
    var direction = EPMS.Web.Resources.Shared.Common.TextDirection;
}

<div class="breadcrumb-container">
    <ul class="xbreadcrumbs">
        <li>
            <a href="/Dashboard/Index">
                <i class="icon-photon home"></i>
            </a>
        </li>
        <li>
            <a href="">
                @EPMS.Web.Resources.HR.JobTitle.Label
            </a>
        </li>
    </ul>
</div>
<header>
    <i class="icon-big-notepad" ></i>
    <h2><small>@ViewBag.Title</small></h2>
    <h3> <small><a href="~/HR/JobTitle/Create">@EPMS.Web.Resources.HR.JobTitle.AddNew</a></small></h3>
</header>

<!--Notifications begin-->
@Html.Partial("_Alert")
<!--Notifications end-->
<form class="form-horizontal">
    <div id="English" class="container-fluid">

        <div class="row-fluid">
            <div class="span12">
                <table id="JobTitle" class="table table-striped table-responsive">
                    <thead>
                        <tr>
                            <th></th>
                            <th>@EPMS.Web.Resources.HR.JobTitle.Name</th>
                            <th>@EPMS.Web.Resources.HR.JobTitle.Department</th>
                            <th>@EPMS.Web.Resources.HR.JobTitle.NoOfEmployees</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.JobTitleList.ToList())
                        {
                            <tr>
                                <td>@item.JobTitleId</td>
                                @if (direction == "ltr")
                                {
                                    <td>@Html.ActionLink(@item.JobTitleNameE, "Create", new { id = item.JobTitleId }) </td>
                                }
                                else
                                {
                                    <td>@Html.ActionLink(@item.JobTitleNameA, "Create", new { id = item.JobTitleId }) </td>
                                }
                                @if (direction == "ltr")
                                {
                                    <td>@item.DepartmentNameE</td>
                                }
                                else
                                {
                                    <td>@item.DepartmentNameA</td>
                                }
                                <td>@item.EmployeesCount</td>


                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</form>

<script>
    $(document).ready(function () {
        $('#JobTitle').dataTable({
            "sPaginationType": "bootstrap",
            "aoColumns": [
                { "bVisible": false }, // cloumn to be hide
                  null, // not hide
                  null,
                  null
            ],
            "fnInitComplete": function () {
                $(".dataTables_wrapper select").select2({
                    dropdownCssClass: 'noSearch'
                });
            }
        });
        $("#simpleSelectBox").select2({
            dropdownCssClass: 'noSearch'
        });
    });
</script>