@using System.Configuration
@model EPMS.WebModels.ViewModels.Receipt.ReceiptListViewModel

@{
    ViewBag.Title = EPMS.WebModels.Resources.CMS.Receipt.List;
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.MessageVM = TempData["message"];
    var direction = EPMS.WebModels.Resources.Shared.Common.TextDirection;
}

<div class="breadcrumb-container">
    <ul class="xbreadcrumbs">
        <li>
            <a href="/Dashboard/Index">
                <i class="icon-photon home"></i>
            </a>
        </li>
        <li>
            <a href="">
                @EPMS.WebModels.Resources.CMS.Receipt.List
            </a>
        </li>
    </ul>
</div>
<header>
    <i class="icon-big-notepad"></i>
    <h2><small>@ViewBag.Title</small></h2>
</header>

<!--Notifications begin-->
@Html.Partial("_Alert")
<!--Notifications end-->


<form class="form-horizontal">
    <div id="English" class="container-fluid">
        <div class="row-fluid">
            <div class="span12">
                
                <table id="ReceiptTable" class="table table-striped table-responsive">
                    <thead>
                        <tr>
                            <th>@EPMS.WebModels.Resources.CMS.Receipt.Receipts</th>
                            <th>@EPMS.WebModels.Resources.CMS.Receipt.Invoice</th>
                            <th>@EPMS.WebModels.Resources.CMS.Invoice.ClientName</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var receipt in Model.Receipts.ToList())
                        {
                            <tr>
                                <td>@Html.ActionLink(@receipt.ReceiptNumber.ToString(), "Detail", new { id = receipt.ReceiptId }) </td>
                                <td><a href="@(ConfigurationManager.AppSettings["SiteURL"])/CMS/Invoice/Detail/@receipt.InvoiceId">@receipt.InvoiceNumber</a></td>
                                <td>
                                    @if (direction == "ltr")
                                    {
                                        <a href="@(ConfigurationManager.AppSettings["SiteURL"])/CMS/Customer/Details/@receipt.CustomerId">@receipt.CustomerNameE</a>
                                    }
                                    else
                                    {
                                        <a href="@(ConfigurationManager.AppSettings["SiteURL"])/CMS/Customer/Details/@receipt.CustomerId">@receipt.CustomerNameA</a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</form>

<script>
    $(document).ready(function () {
        $('#ReceiptTable').dataTable({
            "sPaginationType": "bootstrap",
            "aoColumns": [
                  null,
                  null,
                  null
            ],
            "fnInitComplete": function () {
                $(".dataTables_wrapper select").select2({
                    dropdownCssClass: 'noSearch'
                });
            },
        });
        $("#simpleSelectBox").select2({
            dropdownCssClass: 'noSearch'
        });
    });
</script>