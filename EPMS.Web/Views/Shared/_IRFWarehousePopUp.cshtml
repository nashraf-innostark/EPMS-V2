@model EPMS.WebModels.ViewModels.IRF.ItemReleaseCreateViewModel
@using iTextSharp.text.pdf.qrcode
@{
    var direction = EPMS.WebModels.Resources.Shared.Common.TextDirection;
}
<div id="warhouseSelector" class="modal hide fade treeSelector">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h3>Warhouse</h3>
    </div>
    <div class="modal-body ">
        <form class="form-horizontal warhouseSelection">
            <input id='WarehouseModalIndex' type='hidden' />
            <input id='WarehouseCount' type='hidden' />
            <div id="Simple_Multiple_Select_Box" class="control-group row-fluid">
                <div class="span4">
                    <label class="control-label" for="MaritalStatus">Select one or more Warhouses </label>
                </div>
                <div class="span8">
                    <div class="controls">
                        <select multiple name="simpleMulti" id="MultiWarehouse"></select>
                    </div>
                </div>

            </div>
            <div class="control-group row-fluid">
                <div class="span12 span-inset">
                    <button type="button" class="btn addWar">Add Warhouse</button>
                </div>
            </div>
            <div class="control-group row-fluid">
                <label id="WarehouseQtyExceedError" class="required center"></label>
                <label id="WarehouseQtyNullError" class="required center"></label>
            </div>
            
            @*@if (Model.ItemReleaseDetails[0].ItemReleaseQuantities.Any())
                {
                    for (int i = 0; i < Model.ItemReleaseDetails[0].ItemReleaseQuantities.Count(); i++)
                    {
                        <div class="control-group row-fluid warhouses">

                            @Html.HiddenFor(model => model.ItemReleaseDetails[0].ItemReleaseQuantities[i].ItemVariationId)
                            <div class="span3">
                                <label class="control-label" for="quantity"> @Model.ItemReleaseDetails[0].ItemReleaseQuantities[i].Warehouse.WarehouseNumber </label>
                            </div>
                            <div class="span7">
                                <div class="controls">
                                    @Html.TextBoxFor(model => model.ItemReleaseDetails[0].ItemReleaseQuantities[i].Quantity, new { @class = "" })
                                </div>
                            </div>
                            <div class="span1 span-inset">
                                <button type="button" class="btn dropWar">-</button>
                            </div>
                        </div>
                    }
                }*@
        </form>
    </div>
    <div class="modal-footer">
        <a href="javascript:;" class="btn btn-primary" id="AddWarehouse">Add</a>
        <a href="javascript:;" class="btn" data-dismiss="modal">Cancel</a>
    </div>
</div>